@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sayfa Bulunamadı - Ready Sipariş</title>
    <style>
        body {
            background-color: #f0f2f5; /* Login ekranındaki arka plan rengi */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .error-card {
            background: #ffffff;
            border-radius: 16px; /* Login kartındaki oval köşeler */
            box-shadow: 0 10px 25px rgba(0,0,0,0.05); /* Hafif gölge */
            padding: 40px;
            width: 100%;
            max-width: 400px;
            text-align: center;
            border: none;
        }

        .icon-box {
            width: 80px;
            height: 80px;
            background-color: #0d6efd; /* Ready logosunun mavisi */
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            margin: 0 auto 20px auto;
            box-shadow: 0 4px 10px rgba(13, 110, 253, 0.3);
        }

        .btn-ready {
            background-color: #0d6efd;
            border-color: #0d6efd;
            color: white;
            border-radius: 8px;
            padding: 12px 20px;
            font-weight: 600;
            width: 100%;
            transition: all 0.3s ease;
        }

            .btn-ready:hover {
                background-color: #0b5ed7;
                transform: translateY(-2px);
            }

        .text-muted-custom {
            color: #6c757d;
            font-size: 0.95rem;
            margin-bottom: 30px;
        }

        h3 {
            color: #333;
            font-weight: 700;
            margin-bottom: 10px;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
</head>
<body>

    <div class="error-card">
        <div class="icon-box" style="background-color: #dc3545;">
            <i class="fa-solid fa-triangle-exclamation"></i>
        </div>

        <h3>Sayfa Bulunamadı</h3>
        <p class="text-muted-custom">
            Aradığınız sayfa silinmiş, taşınmış veya hiç var olmamış olabilir.
        </p>

        
    </div>

</body>
</html>